version: "2"
services:
  app:
    container_name: app
    restart: always
    image: node:latest
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data/db
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo:latest
    ports:
      - "27017:27017"